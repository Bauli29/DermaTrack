spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    open-in-view: false
  h2:
    console:
      enabled: false
  security:
    user:
      name: testuser
      password: testpass

app:
  auth:
    username: testuser
    password: testpassword
  jwt:
    access-secret: test-access-secret-test-access-secret-32bytes!
    refresh-secret: test-refresh-secret-test-refresh-secret-32bytes!
  mockdata:
    enabled: false

logging:
  level:
    de.dermatrack: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
